<!DOCTYPE html>
<html lang="en">
<div class = "body-container">
      <div class = "container">
  <head>

    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/jquery.slick/1.3.11/slick.css"/>

      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />

      <title><%= content_for?(:title) ? yield(:title) : "ShieldED" %></title>

      <%= stylesheet_link_tag    "application" %>
      <%= javascript_include_tag "vendor/modernizr" %>
      <%= csrf_meta_tags %>

      <header>
            <%=image_tag("shield_L-01.png", width: "400")%>
      </header>

  </head>

  <body>

      <div class="navbar">
        <ul class = "home-navbar-links">
          <li>
            <% if !signed_in? %>
              <%= link_to("Login", login_partial_path, class: "Login_Link") %></li>
            <% end %>
            <% if signed_in? %>
              <%= link_to("Logout",logout_path) %>
            <% end %>
          </li>
          <li><a href = "mailto:andrew.mcclellan3@gmail.com">Contact Us</a></li>
          <li> <%= link_to("Report an Incident", new_event_path, class: "report-event")%></a></li>
          <li><a href = "">Local Resources</a></li>
          <li><a class = "navbar-admin-link" href="#" data-reveal-id="myModal">Admin</a></li>
        </ul>
      </div>

      <%= yield %>

  <div class = "placeholder">
    <div class = "introduction">
    <h4>Why We're Here</h4>
    <p>Every day there are students across the country that become victims of bullying while at school.  Bullying comes in a variety of forms, and is often overlooked by a victim's peers. Shame and fear often prevent this abuse from being reported, and can leave some victim's helpless to protect themselves.<p>
    <h4>What We Can Do </h4>
    <p>Shield-ED is a new line of defense in protecting every student's right to a learning environment that is respectful and safe.  By providing an anonymous reporting system, we enable students and their parents with a means to fight back against bullying.</p>
    </div>
  </div>

  <footer>
    <ul>
      <li><p>©2014 Team Smashing. Andrew. Ruskin. Anders.</p></li>
    </ul>
  </footer>

    <%= javascript_include_tag "application" %>
    <script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">google.load("visualization", "1", {packages:["corechart"]});
    </script>

        <div id="myModal" class="reveal-modal" data-reveal>
            <%=image_tag("OrangeShieldLogo.png", width: "100", :class => "logo")%>
            <h3>Admin Login</h3>
            <%= form_tag({:controller => 'sessions', :action => 'create'}, { :class => "admin-login-form"}) do %>
              <%= label_tag(:username, "Username: ") %>
              <%= text_field_tag(:username) %>
              <%= label_tag(:password_digest, "Password: ") %>
              <%= password_field_tag(:password_digest) %>
              <p class = "error"><%=session[:error]%></p>
              <%= submit_tag("login", :class => "admin-login-button")  %>
            <% end %>
          <a class="close-reveal-modal">&#215;</a>
        </div>

  </body>
</div>
</div>
</html>




